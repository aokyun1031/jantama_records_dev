services:
  - type: web
    name: jantama-records
    env: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PGSSLMODE
        value: require

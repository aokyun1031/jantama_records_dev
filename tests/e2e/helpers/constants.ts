/**
 * テストデータ識別用プレフィックス。
 * globalTeardown はこのプレフィックスに前方一致する players / tournaments を一括削除する。
 */
export const TEST_PREFIX = '__E2E_TEST_';

/**
 * 404 検証で使う、実在しないはずの ID。
 */
export const NONEXISTENT_ID = 999999;
